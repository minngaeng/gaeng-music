import { sleep } from '@/lib/utils';
import React from 'react';

const page = async () => {
  // await sleep(1000);

  // throw new Error('error');
  return <div>homepage</div>;
};

export default page;
